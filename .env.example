# 개발: Vite 프록시 사용 (vite.config.ts 의 server.proxy 가 실제 호스팅으로 포워딩)
# 운영: 프론트를 API 와 동일 도메인에 배포하면 그대로 사용 가능
VITE_API_BASE_URL=/typrain_api
# 배틀 실시간 서버(typrun-ws). 미설정 시 dev 폴백 ws://localhost:3001/ws
# VITE_WS_URL=ws://localhost:3001/ws
